# Codex Navo v1.2.92

## Fixed

- The floating window now reads API Codex input, cache, and output values from today's local-day bucket instead of the API Key lifetime total.
- Floating-window usage automatically resets at local midnight and remains synchronized with the Today view in the main application.
