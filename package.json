{
  "name": "codex-navo",
  "productName": "Codex Navo",
  "private": true,
  "version": "1.1.28",
  "description": "A local Windows account environment switcher for Codex.",
  "main": "desktop-src/main.js",
  "author": "1080ssf",
  "license": "MIT",
  "dependencies": {
    "electron-updater": "^6.6.2"
  },
  "devDependencies": {
    "electron": "^43.2.0",
    "electron-builder": "^26.0.12"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "electron .",
    "test": "node --test test/*.test.js",
    "test:smoke": "node test/smoke.js",
    "build:desktop": "electron-builder --win nsis --x64 --publish never"
  },
  "build": {
    "appId": "com.codexswitchboard.app",
    "productName": "Codex Navo",
    "asar": true,
    "directories": {
      "output": "release/auto-update"
    },
    "files": [
      "desktop-src/**/*",
      "server.js",
      "lib/**/*",
      "public/**/*",
      "config/settings.example.json",
      "README.md",
      "LICENSE",
      "package.json"
    ],
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "desktop-src/icon.ico",
      "artifactName": "Codex-Navo-Setup-${version}-windows-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "deleteAppDataOnUninstall": false
    },
    "publish": {
      "provider": "github",
      "owner": "1080ssf",
      "repo": "codex-navo"
    }
  }
}
